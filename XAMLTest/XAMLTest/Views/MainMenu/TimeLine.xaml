<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XAMLTest.Views.MainMenu"
             NavigationPage.HasNavigationBar="False"
             x:Class="XAMLTest.Views.MainMenu.TimeLine">
    
    <ContentPage.Content>
            <FlexLayout Direction="Column">
                <!-- Header -->
            <Frame TranslationY="-120">
                <FlexLayout>
                <Image Source="Tijdlijn1.png"/>
                <Image Source="Tijdlijn2.png"/>
                <Image Source="Tijdlijn3.png"/>
                </FlexLayout>
            </Frame>
            <Frame>
            <Entry x:Name="TimelineTweet" Placeholder="Wat wil je delen?" MaxLength="140" />
            </Frame>
            <!-- Body -->
            <Frame>
                <FlexLayout FlexLayout.Grow="1">
                    <!-- Navigation items-->
                    <Frame>
                        <FlexLayout>
                            <Image Source="Tijdlijn18.png" />
                        </FlexLayout>
                    </Frame>
                    <!-- Content -->
                    <ListView x:Name="TimeLineView" BindingContext="{Binding MyNiceObject}" FlexLayout.Grow="1">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextCell Text="{Binding MyNiceObject}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>


                </FlexLayout>
            </Frame>
            <!-- Footer -->
            <Frame>
                <FlexLayout>
                    <Image Source="Tijdlijn4.png" />
                    <Image Source="Tijdlijn5.png" />
                    <Image Source="Tijdlijn6.png" />
                    <Image Source="Tijdlijn7.png" />
                </FlexLayout>
            </Frame>
        </FlexLayout>
            <!--
            <Entry x:Name="TimelineTweet" Placeholder="Wat wil je delen?" MaxLength="140" />
            <Button Text="Profile"  Clicked="OnItemClicked"/><ListView x:Name="TimeLineView" ItemsSource=""></ListView>
            <ListView x:Name="TimeLineView" BindingContext="{Binding MyNiceObject}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding MyNiceObject}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView> -->
    </ContentPage.Content>
    
</ContentPage>